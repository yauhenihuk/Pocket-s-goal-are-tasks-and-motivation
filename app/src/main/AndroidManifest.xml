<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pocketsgoalaretasksandmotivation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activities.goals.GoalSetup"></activity>
        <activity android:name=".activities.goals.Others"></activity>
        <activity android:name=".activities.goals.Financial"></activity>
        <activity android:name=".activities.goals.Career"></activity>
        <activity android:name=".activities.goals.Business"></activity>
        <activity android:name=".activities.goals.Health"></activity>
        <activity android:name=".activities.goals.Sport"></activity>
        <activity android:name=".activities.TargetsAndGoals"></activity>
        <activity android:name=".activities.MainActivity"></activity>
        <activity android:name=".activities.GoalsMain"></activity>
        <activity android:name=".activities.DateOfBirthday">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN"
                    android:screenOrientation="portrait" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.GoalsSetup"
            android:screenOrientation="portrait" />
    </application>

</manifest>